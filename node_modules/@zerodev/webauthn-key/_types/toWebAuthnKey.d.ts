import { type Hex } from "viem";
export declare enum WebAuthnMode {
    Register = "register",
    Login = "login"
}
export type WebAuthnKey = {
    pubX: bigint;
    pubY: bigint;
    authenticatorId: string;
    authenticatorIdHash: Hex;
};
export type WebAuthnAccountParams = {
    passkeyName: string;
    passkeyServerUrl: string;
    rpID?: string;
    webAuthnKey?: WebAuthnKey;
    mode?: WebAuthnMode;
    credentials?: RequestCredentials;
    passkeyServerHeaders: Record<string, string>;
};
export declare const encodeWebAuthnPubKey: (pubKey: WebAuthnKey) => `0x${string}`;
export declare const toWebAuthnKey: ({ passkeyName, passkeyServerUrl, rpID, webAuthnKey, mode, credentials, passkeyServerHeaders }: WebAuthnAccountParams) => Promise<WebAuthnKey>;
//# sourceMappingURL=toWebAuthnKey.d.ts.map